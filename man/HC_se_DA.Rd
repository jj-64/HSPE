% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Dagum.R
\name{HC_se_DA}
\alias{HC_se_DA}
\title{Standard Errors for Dagum Poverty Headcount via Delta Method}
\usage{
HC_se_DA(y, a, b, p, se_a, se_b, se_p)
}
\arguments{
\item{y}{Numeric vector of poverty lines.}

\item{a}{Estimated Dagum shape parameter.}

\item{b}{Estimated Dagum scale parameter.}

\item{p}{Estimated Dagum shape parameter.}

\item{se_a}{Standard error of \code{a}.}

\item{se_b}{Standard error of \code{b}.}

\item{se_p}{Standard error of \code{p}.}
}
\value{
Numeric vector of standard errors, same length as \code{y}.
}
\description{
Computes **delta-method standard errors** for headcount poverty indices
using the Dagum CDF:

\deqn{
H(z) = F(z \mid a, b, p)
  = \left( 1 + (z/b)^{-a} \right)^{-p}
}

The delta-method approximation is:

\deqn{
\mathrm{Var}(H)
  \approx
  \nabla_\theta H^\top
  \Sigma_\theta
  \nabla_\theta H,
}

with parameters \eqn{\theta = (a, b, p)}.
}
\details{
- This implementation assumes **independent parameter estimates**
  (diagonal covariance matrix).
- If a full vcov matrix is available, the function can be extended easily.
- Computes the analytical gradient of the Dagum CDF wrt \eqn{a,b,p}.
}
\examples{
y <- c(1, 2, 3)
HC_se_DA(
  y,
  a = 2, b = 1, p = 3,
  se_a = 0.05, se_b = 0.02, se_p = 0.03
)

}
