% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Beta2.R
\name{HC_se_B2}
\alias{HC_se_B2}
\title{Standard Errors for Beta-2 Poverty Headcount via Delta Method}
\usage{
HC_se_B2(y, b, p, q, se_b, se_p, se_q, eps = 1e-06)
}
\arguments{
\item{y}{Numeric vector of poverty lines.}

\item{b}{Scale parameter estimate.}

\item{p}{First shape parameter estimate.}

\item{q}{Second shape parameter estimate.}

\item{se_b}{Standard error of \code{b}.}

\item{se_p}{Standard error of \code{p}.}

\item{se_q}{Standard error of \code{q}.}

\item{eps}{Step size for numerical gradient (default 1e-6).}
}
\value{
A numeric vector of standard errors (same length as \code{y}).
}
\description{
Computes \strong{delta-method standard errors} for poverty headcount measures
based on the CDF of the \strong{Beta-2 (Beta Prime)} distribution.

For a poverty line \eqn{z}, the headcount is:

\deqn{
H(z) = F(z \mid p, q, b)
}

and the delta-method variance is:

\deqn{
\mathrm{Var}(H) \approx \nabla_\theta H^\top \Sigma_\theta \nabla_\theta H
}

where \eqn{\theta = (p, q, b)}.
}
\details{
The function:
\itemize{
\item constructs a diagonal covariance matrix if only SEs are provided
\item uses Richardson numerical differentiation
\item returns one SE per poverty line
}
}
\examples{
y <- c(1, 2, 3)
HC_se_B2(
  y, b = 2, p = 3, q = 4,
  se_b = 0.1, se_p = 0.05, se_q = 0.06
)

}
